# Use node_modules so Prisma and other tools resolve correctly
nodeLinker: node-modules

# Avoid install failures on Railway (Linux) when lockfile was generated on Windows;
# checksums can differ across platforms. Use "ignore" so install succeeds in CI.
checksumBehavior: ignore