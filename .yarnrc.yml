# Use node_modules so Prisma and other tools resolve correctly (no PnP)
nodeLinker: node-modules

# Avoid install failures on Railway (Linux) when lockfile was generated on Windows
checksumBehavior: ignore

# Allow install to succeed when lockfile would otherwise be modified (e.g. in CI)
enableImmutableInstalls: false
